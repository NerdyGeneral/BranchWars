*** Begin Patch
*** Update File: src/ui/dashboard.js
@@
-function renderCompetitiveActions(v){
- const quote=E.planBudget(v.me,draft),rival=v.competitiveActions[v.rival.lastCompetitiveAction]||v.competitiveActions.none;
- $('#competitiveBrief').innerHTML=`<div><span>FOCUS MARKET</span><b>${esc(v.territories[draft.focus].name)}</b></div><div><span>COMMITTED SPEND · ENTIRE PLAN</span><b>${money(quote.total)}</b><div class="micro muted">Action ${money(quote.action)} · Initiatives ${money(quote.projects)} · Research ${money(quote.research)} · Recruiting ${money(quote.recruiting)}</div></div><div><span>LAST RIVAL ACTION</span><b>${esc(rival.name)}</b><div class="micro muted">Known only after resolution</div></div>`;
- $('#competitiveActions').innerHTML=Object.entries(v.competitiveActions).map(([key,a])=>{const selected=draft.competitiveAction===key,poor=quote.total-quote.action+a.cost>quote.cash,influence=a.minInfluence&&v.me.stats.influence<a.minInfluence,disabled=v.me.submitted||(!selected&&(poor||influence)),why=poor?'Not enough uncommitted cash. Reduce another commitment first.':influence?`Requires ${a.minInfluence} influence.`:a.desc;return`<button type="button" class="competitive-action ${selected?'selected':''}" data-competitive-action="${key}" ${disabled?'disabled':''}><span class="action-head"><span>${esc(a.name)}</span><span>${money(a.cost)}</span></span><span class="action-meta">${esc(a.kind.toUpperCase())} · COUNTER: ${esc(a.counter)}</span><span class="action-meta">${esc(why)}</span></button>`}).join('');
- $$('[data-competitive-action]').forEach(b=>b.addEventListener('click',()=>{draft.competitiveAction=b.dataset.competitiveAction;renderProjects(v);renderReady(v)}))
-}
-
+function renderCompetitiveActions(v){
+ const quote=E.planBudget(v.me,draft),rival=v.competitiveActions[v.rival.lastCompetitiveAction]||v.competitiveActions.none;
+ $('#competitiveBrief').innerHTML=`<div><span>FOCUS MARKET</span><b>${esc(v.territories[draft.focus].name)}</b></div><div><span>COMMITTED SPEND · ENTIRE PLAN</span><b>${money(quote.total)}</b><div class="micro muted">Action ${money(quote.action)} · Initiatives ${money(quote.projects)} · Research ${money(quote.research)} · Recruiting ${money(quote.recruiting)}</div></div><div><span>LAST RIVAL ACTION</span><b>${esc(rival.name)}</b><div class="micro muted">Known only after resolution</div></div>`;
+ $('#competitiveActions').innerHTML=Object.entries(v.competitiveActions).map(([key,a])=>{const selected=draft.competitiveAction===key,poor=quote.discretionaryRemaining!==undefined?(key==='none'?false:quote.discretionaryRemaining+quote.action-a.cost<0):quote.total-quote.action+a.cost>quote.cash,influence=a.minInfluence&&v.me.stats.influence<a.minInfluence,disabled=v.me.submitted||(!!v.me.departmentOffice&&!!v.gameOver)||(!selected&&(poor||influence)),why=poor?(quote.discretionaryRemaining!==undefined?'Not enough optional spending room after existing obligations. Reduce another optional commitment first.':'Not enough uncommitted cash. Reduce another commitment first.'):influence?`Requires ${a.minInfluence} influence.`:a.desc;return`<button type="button" class="competitive-action ${selected?'selected':''}" data-competitive-action="${key}" ${disabled?'disabled':''}><span class="action-head"><span>${esc(a.name)}</span><span>${money(a.cost)}</span></span><span class="action-meta">${esc(a.kind.toUpperCase())} · COUNTER: ${esc(a.counter)}</span><span class="action-meta">${esc(why)}</span></button>`}).join('');
+ const campaign=v.me.departmentOffice?(game||view):null,signature=v.me.departmentOffice?JSON.stringify(draft):null;
+ $$('[data-competitive-action]').forEach(b=>b.addEventListener('click',()=>{
+  const key=b.dataset.competitiveAction;
+  if(v.me.departmentOffice){
+   const now=currentView();
+   if(!now||now.me.id!==v.me.id||now.cycle!==v.cycle||(game||view)!==campaign||JSON.stringify(draft)!==signature||now.me.submitted||now.gameOver)return;
+   const action=now.competitiveActions[key];if(!action)return;
+   const current=E.planBudget(now.me,draft),selected=key===draft.competitiveAction;
+   if(!selected&&key!=='none'&&((current.discretionaryRemaining??current.remaining)+current.action-action.cost<0||(action.minInfluence&&now.me.stats.influence<action.minInfluence)))return;
+  }
+  draft.competitiveAction=key;renderProjects(v);renderReady(v)
+ }))
+}
+
@@
-function renderPlanBudget(v){
- const q=E.planBudget(v.me,draft);
- $('#planBudget').innerHTML=`<span>Committed <b>${money(q.total)}</b></span><span>${v.campaignRulesVersion===1?'Available within cash / capital limits':'Uncommitted cash'} <b class="${q.remaining<0?'bad':''}">${money(q.remaining)}</b></span><span>Execution <b class="${q.freeCapacity<0?'warn':''}">${q.load.toFixed(1)} / ${q.capacity.toFixed(1)}</b></span><span>New base payroll <b>${money(q.basePayrollAdded)}/cycle</b>${v.me.workforce?' + specialist premiums <b>'+money(q.specialistPayrollAdded)+'/cycle</b>':''}</span><details><summary>Spending & timing</summary><div>Initiatives ${money(q.projects)} · Research ${money(q.research)}${v.me.workforce?' · Reserved training '+money(q.training):''} · Recruiting ${money(q.recruiting)} · Competition ${money(q.action)}${v.me.productPrograms?' · Product retirement '+money(q.productRetirement):''}${v.me.advertising?' · Advertising '+money(q.advertising):''}${v.me.relationshipOffers?' · Existing-customer conversions (ceiling) '+money(q.relationshipOffers):''}${v.me.onboarding?' · Onboarding activation (ceiling) '+money(q.onboarding):''}.</div>${v.campaignRulesVersion===1?`<div>Capital-safe spending limit ${money(q.capitalBudget)}: preserves an 8% current exposure reserve. Discretionary spending is expensed, not capitalized.</div>`:''}<div>Uncommitted cash excludes operating income, expenses, event effects, and rival actions. New bankers and capability benefits apply from the next cycle; payroll shown before efficiency adjustments.</div>${q.freeCapacity<0?'<div class="warn">Existing work may stall with this staffing allocation.</div>':''}</details>`;
-}
+function renderPlanBudget(v){
+ const q=E.planBudget(v.me,draft);
+ if(q.discretionaryRemaining!==undefined){
+  const accrued=v.me.accounting.accounts.payables||0,unfunded=Math.max(0,accrued+q.mandatoryObligations-q.cash);
+  $('#planBudget').innerHTML=`<span>Committed · full plan <b>${money(q.total)}</b></span><span>Optional spending room after this plan <b class="${q.discretionaryRemaining<0?'bad':''}">${money(q.discretionaryRemaining)}</b></span><span>Existing monthly obligations <b>${money(q.mandatoryObligations)}</b></span><span>Already accrued payables <b>${money(accrued)}</b></span><span>Execution <b class="${q.freeCapacity<0?'warn':''}">${q.load.toFixed(1)} / ${q.capacity.toFixed(1)}</b></span><span>New base payroll <b>${money(q.basePayrollAdded)}/cycle</b>${v.me.workforce?' + specialist premiums <b>'+money(q.specialistPayrollAdded)+'/cycle</b>':''}</span>
+   ${accrued||unfunded?'<div class="warn" role="status">Obligations remain owed. Already accrued '+money(accrued)+'; unfunded from current cash '+money(unfunded)+'. Unpaid compensation accrues as a liability; it is not forgiven and does not create optional spending money.</div>':''}
+   <details><summary>Spending & timing</summary><div>Initiatives ${money(q.projects)} · Research ${money(q.research)} · Reserved training ${money(q.training||0)} · Recruiting ${money(q.recruiting)} · Competition ${money(q.action)} · Leadership ${money(q.departmentLeadership)} · Office conversion ${money(q.facilityConversion||0)} · Product retirement ${money(q.productRetirement||0)} · Advertising ${money(q.advertising||0)} · Relationship conversions ${money(q.relationshipOffers||0)} · Onboarding ${money(q.onboarding||0)}.</div><div>Optional commitments ${money(q.discretionaryCommitments)}; cash room before those commitments ${money(q.discretionaryCashAvailable)}, capital-safe room ${money(q.discretionaryCapitalAvailable)}. Both subtract existing obligations; cash room also subtracts accrued payables.</div><div>Full-plan remaining before accrual ${money(q.remaining)}. Existing compensation may remain payable when cash is insufficient, but new discretionary appointments and spending still require funding. No operating income, event effects or rival actions are assumed here.</div>${q.freeCapacity<0?'<div class="warn">Existing work may stall with this staffing allocation.</div>':''}</details>`;
+  return;
+ }
+ $('#planBudget').innerHTML=`<span>Committed <b>${money(q.total)}</b></span><span>${v.campaignRulesVersion===1?'Available within cash / capital limits':'Uncommitted cash'} <b class="${q.remaining<0?'bad':''}">${money(q.remaining)}</b></span><span>Execution <b class="${q.freeCapacity<0?'warn':''}">${q.load.toFixed(1)} / ${q.capacity.toFixed(1)}</b></span><span>New base payroll <b>${money(q.basePayrollAdded)}/cycle</b>${v.me.workforce?' + specialist premiums <b>'+money(q.specialistPayrollAdded)+'/cycle</b>':''}</span><details><summary>Spending & timing</summary><div>Initiatives ${money(q.projects)} · Research ${money(q.research)}${v.me.workforce?' · Reserved training '+money(q.training):''} · Recruiting ${money(q.recruiting)} · Competition ${money(q.action)}${v.me.productPrograms?' · Product retirement '+money(q.productRetirement):''}${v.me.advertising?' · Advertising '+money(q.advertising):''}${v.me.relationshipOffers?' · Existing-customer conversions (ceiling) '+money(q.relationshipOffers):''}${v.me.onboarding?' · Onboarding activation (ceiling) '+money(q.onboarding):''}.</div>${v.campaignRulesVersion===1?`<div>Capital-safe spending limit ${money(q.capitalBudget)}: preserves an 8% current exposure reserve. Discretionary spending is expensed, not capitalized.</div>`:''}<div>Uncommitted cash excludes operating income, expenses, event effects, and rival actions. New bankers and capability benefits apply from the next cycle; payroll shown before efficiency adjustments.</div>${q.freeCapacity<0?'<div class="warn">Existing work may stall with this staffing allocation.</div>':''}</details>`;
+}
*** Update File: src/ui/projects.js
@@
-function renderReady(v){if(draft.contractBid&&draft.allocation.business<1){draft.contractBid=null}if(v.serviceAgreements)renderPipeline(v);const pool=unassigned(v),action=v.competitiveActions[draft.competitiveAction],items=[...(v.serviceAgreements?[draft.contractBid?'✓ Service agreement bid':'— Service bid (automatic incumbent defense)']:[]),`${draft.focus?'✓':'○'} Focus market`,`${draft.decision?'✓':'○'} Executive decision`,`${pool===0?'✓':'○'} Staff allocated`,`⚔ ${action.name}`,`${draft.opportunity?'✓':'—'} Priority opportunity (optional)`,`${draft.newProjects&&draft.newProjects.length?`✓ ${draft.newProjects.length} initiative${draft.newProjects.length===1?'':'s'}`:'— Initiatives (optional)'}`,`${E.planHires(draft)?`✓ Hiring ${E.planHires(draft)}`:'— Hiring (optional)'}`,`${Object.keys(draft.investments||{}).length?`✓ Funding ${Object.keys(draft.investments).length} capabilit${Object.keys(draft.investments).length===1?'y':'ies'}`:'— Capability funding (optional)'}`];$('#planChecklist').innerHTML=items.join(' &nbsp; ');const projectStatus=E.projectPlanStatus(v.me,draft),quote=projectStatus.quote,overBudget=quote.remaining<0;renderPlanBudget(v);renderOperatingPreview(v);renderCompetitiveActions(v);renderWorkforce(v);renderProductPrograms(v);$('#readyBtn').disabled=v.me.submitted||!planReady(v)||overBudget||!projectStatus.eligible;$('#recallBtn').classList.toggle('hidden',!v.me.submitted||v.rival.submitted||v.gameOver);$('#submitMsg').textContent=v.me.submitted?'PLAN LOCKED // WAITING FOR RIVAL':overBudget?`This plan commits ${money(quote.total)} but ${v.campaignRulesVersion===1?money(quote.capitalBudget)+' is available within capital limits':'only '+money(quote.cash)+' is available'}. Reduce funding, initiatives, hiring or the competitive action.`:!projectStatus.eligible?projectStatus.reason:'The market remains paused until both institutions are ready.';$('#submitMsg').className='small '+(overBudget||!projectStatus.eligible?'bad':'muted')}
+function renderReady(v){if(draft.contractBid&&draft.allocation.business<1){draft.contractBid=null}if(v.serviceAgreements)renderPipeline(v);const pool=unassigned(v),action=v.competitiveActions[draft.competitiveAction],items=[...(v.serviceAgreements?[draft.contractBid?'✓ Service agreement bid':'— Service bid (automatic incumbent defense)']:[]),`${draft.focus?'✓':'○'} Focus market`,`${draft.decision?'✓':'○'} Executive decision`,`${pool===0?'✓':'○'} Staff allocated`,`⚔ ${action.name}`,`${draft.opportunity?'✓':'—'} Priority opportunity (optional)`,`${draft.newProjects&&draft.newProjects.length?`✓ ${draft.newProjects.length} initiative${draft.newProjects.length===1?'':'s'}`:'— Initiatives (optional)'}`,`${E.planHires(draft)?`✓ Hiring ${E.planHires(draft)}`:'— Hiring (optional)'}`,`${Object.keys(draft.investments||{}).length?`✓ Funding ${Object.keys(draft.investments).length} capabilit${Object.keys(draft.investments).length===1?'y':'ies'}`:'— Capability funding (optional)'}`];$('#planChecklist').innerHTML=items.join(' &nbsp; ');const projectStatus=E.projectPlanStatus(v.me,draft),quote=projectStatus.quote,overBudget=(quote.discretionaryRemaining??quote.remaining)<0;renderPlanBudget(v);renderOperatingPreview(v);renderCompetitiveActions(v);renderWorkforce(v);renderProductPrograms(v);$('#readyBtn').disabled=v.me.submitted||(!!v.me.departmentOffice&&!!v.gameOver)||!planReady(v)||overBudget||!projectStatus.eligible;$('#recallBtn').classList.toggle('hidden',!v.me.submitted||v.rival.submitted||v.gameOver);$('#submitMsg').textContent=v.me.submitted?'PLAN LOCKED // WAITING FOR RIVAL':overBudget?(quote.discretionaryRemaining!==undefined?`Optional commitments ${money(quote.discretionaryCommitments)} exceed available cash or capital room. Existing obligations ${money(quote.mandatoryObligations)} remain owed; reduce optional spending.`:`This plan commits ${money(quote.total)} but ${v.campaignRulesVersion===1?money(quote.capitalBudget)+' is available within capital limits':'only '+money(quote.cash)+' is available'}. Reduce funding, initiatives, hiring or the competitive action.`):!projectStatus.eligible?projectStatus.reason:'The market remains paused until both institutions are ready.';$('#submitMsg').className='small '+(overBudget||!projectStatus.eligible?'bad':'muted')}
*** End Patch
